#include<iostream>
using namespace std;

int main()
{
    int T; cin >> T;
    while(T--)
    {
        string str; cin >> str;

        string stn;
        for(auto c : str)
        {
            if(c )
        }

    }

    return 0;
}